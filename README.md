## Conversational Q&A ChatBot With Message History

`chatbot.ipynb` contains the full chatbot code.

`Explanation.ipynb` contains a detailed explanation of the major functions used to build the chatbot.

### Setup

Install the required packages with:

```bash
pip install -r requirements.txt
```
